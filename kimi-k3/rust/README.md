# Rust port

Idiomatic Rust crate `kimi-k3-ref` mirroring the Python reference modules.

```bash
cargo test
```

Sources under `src/`; integration tests in `tests/goldens.rs` load `../fixtures/goldens.json`.
