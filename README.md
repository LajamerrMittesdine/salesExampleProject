# salesExampleProject

Example project (legacy `sales.c` / `sales.h`) plus a scientific ML experiment.

## MD5 learnability experiment

See [`md5_experiment/README.md`](md5_experiment/README.md).

Quick start:

```bash
pip install -r md5_experiment/requirements.txt
python -m md5_experiment.run_experiment --quick
```
